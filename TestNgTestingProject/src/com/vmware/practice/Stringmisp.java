package com.vmware.practice;

import java.util.ArrayList;
import java.util.List;

public class Stringmisp {
	public static void main(String[] args) {
		/*String str = "missippm";
		int k=0;
		List<Character> resStr = new ArrayList<Character>();
		char temp=' ';
		for(int i=0;i<=str.length()-1;i++)
		{
			if(resStr.contains(str.charAt(i)))
					{
						
						temp = temp+str.charAt(i);
						if(temp.contains(str.charAt(i)))
								{
							System.out.println(str.charAt(i)+" is duplicate");
							k++;
								}
					}
			else
			{
				resStr.add(str.charAt(i));
			}*/
			
		}
	//	System.out.println(resStr);
	

}
